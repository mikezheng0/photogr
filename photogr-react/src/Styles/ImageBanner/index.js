import styled from 'styled-components'
import {colours} from '../Constants'

export const ImageContainer = styled.div`
  text-align:center;
`

export const BannerContainer = styled.div`
  background-color: ${colours.blackBackdrop};
  color:white;
`